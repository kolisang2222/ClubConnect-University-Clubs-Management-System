# ClubConnect-University-Clubs-Management-System
ClubConnect is a Java Swing desktop application for managing university clubs. It supports role-based access (Admin, Club Leader, Member, Guest), club and membership management, event scheduling with resource booking, budget requests and approvals, notifications, reporting, and MySQL data persistence with automatic import/export.
